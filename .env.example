# These examples do not require an LLM provider API key.
#
# Increase or decrease this value to make the retry examples fail a different
# number of times before succeeding.
FAIL_FIRST_N_ATTEMPTS=2

# Set this above the retry limit to see the error handler path.
# Example: FAIL_FIRST_N_ATTEMPTS=5
